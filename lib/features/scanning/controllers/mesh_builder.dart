class MeshBuilder {
  void addPointCloudData(List<dynamic> data) {
    // add to accumulator
  }
  
  Future<String> buildAndSaveMesh() async {
    // processes accumulated data and saves to .obj
    return "path/to/mesh.obj";
  }
}
